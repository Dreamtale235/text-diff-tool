# 文本对比工具（Diff Checker）项目文档

> 版本：v1.1（最终维护版） | 类型：纯前端单页工具 | 技术栈：HTML + CSS + 原生 JS

---

## 一、项目定位

一个即开即用的本地浏览器文本对比工具。用户可以粘贴两段文本，或读取文件中的文字内容，查看差异高亮。

**主要用途**：个人临时比较代码、文稿或学习材料。项目作为免费、自用和开源小工具维护，不以商业化为目标。

**一句话描述**：粘贴左边，粘贴右边，哪里不一样一眼看出来。

---

## 二、功能清单

### v1.1 最终功能

| 序号 | 功能 | 描述 |
|------|------|------|
| F1 | 双栏文本输入 | 左右两个文本框，分别粘贴原始文本和修改后文本 |
| F2 | 双模式对比 | 「按行」模式逐行高亮，适合代码对比；「按字」模式逐字符高亮，适合文稿校对 |
| F3 | 按行差异高亮 | 点击对比后，新增行绿色背景、删除行红色背景、相同行无背景 |
| F4 | 按字差异高亮 | 字符级 LCS 算法 + 跨行识别，插入一个字不会导致后续行全部高亮 |
| F5 | 行号显示 | 左右两侧分别显示行号 |
| F6 | 同步滚动 | 左右两个结果面板同步滚动 |
| F7 | 一键清空 | 清空两边内容重新开始 |
| F8 | 交换左右 | 一键交换左右两边内容 |
| F9 | 差异统计 | 显示新增/删除/未修改行数统计 |
| F10 | 复制结果 | 一键复制对比结果到剪贴板 |
| F11 | 文件上传与拖入 | 支持常见纯文本文件，以及 DOCX、XLSX、XLS |
| F12 | 本地处理提示 | 明确文件处理位置、联网依赖和格式比较边界 |

### 明确不再扩展的功能

- 账号、云端存储和多人协作
- PDF、图片或完整 Office 格式比较
- 忽略空白、忽略大小写和复杂导出
- 商业化、订阅或用户数据采集

---

## 三、界面布局

```
┌─────────────────────────────────────────────────────┐
│  🔍 文本对比工具                                     │
│  粘贴两端文本，秒看差异                               │
├──────────────────────────┬──────────────────────────┤
│  原始文本                │  修改后文本               │
│  ┌───────────────────┐   │  ┌───────────────────┐   │
│  │                   │   │  │                   │   │
│  │  (textarea)       │   │  │  (textarea)       │   │
│  │                   │   │  │                   │   │
│  │                   │   │  │                   │   │
│  └───────────────────┘   │  └───────────────────┘   │
│                          │                          │
├──────────────────────────┴──────────────────────────┤
│  [🔍 开始对比]  [⇄ 交换]  [🗑 清空]                  │
├─────────────────────────────────────────────────────┤
│  对比结果: +12 新增  -5 删除  =30 未变               │
├──────────────────────────┬──────────────────────────┤
│  原始文本 (行号)          │  修改后文本 (行号)         │
│  1  相同行               │  1  相同行               │
│  2  -删除行 (红色)       │  2  +新增行 (绿色)       │
│  3  相同行               │  3  +新增行 (绿色)       │
│  4  相同行               │  4  相同行               │
│  ...                     │  ...                     │
├──────────────────────────┴──────────────────────────┤
│  [📋 复制对比结果]                                    │
└─────────────────────────────────────────────────────┘
```

---

## 四、技术方案

| 项目 | 选择 |
|------|------|
| 结构 | 单 HTML 文件（含内联 CSS + JS） |
| 差异算法 | 基于 LCS（最长公共子序列）的逐行比较 |
| 数据存储 | 无持久化需求（不涉及 localStorage） |
| 图表 | 不需要 |
| 外部依赖 | Mammoth.js 1.8.0、SheetJS 0.18.5（固定版本 CDN） |
| 文件大小 | 主 HTML 约 40KB，不含外部解析库 |
| 隐私边界 | 对比和文件解析在浏览器中完成；DOCX/Excel 解析库首次加载需要联网 |
| 文件语义 | DOCX 只提取文字；Excel 转为 CSV 文本；不比较格式、版式、批注或修订记录 |

## 五、文件结构

```
HTML文本对比工具——测试/
├── README.md                （GitHub 项目说明）
├── 纯前端工具开发建议.md   （已存在）
├── Diff项目文档.md          （本文件）
├── diff-checker.html        （独立产品文件）
└── index.html               （GitHub Pages 入口，与产品文件内容一致）
```

## 六、验收标准

- [x] 粘贴两段不同文本，点击对比，正确标出新增行（绿）和删除行（红）
- [x] 按字模式保持原始行边界，并正确统计修改行
- [x] 粘贴两段完全相同文本，统计为全部未变
- [x] 左边或右边为空时仍能完成对比
- [x] 左右面板同步滚动
- [x] 移动端布局正常（文本框上下排，不并排）
- [x] 纯文本文件上传可用
- [x] 页面无业务控制台错误

## 七、维护状态

本项目已进入仅修复严重缺陷的维护状态。最终维护版重点保证基本比较正确、产品边界透明和源码可直接部署，不再增加新功能。
